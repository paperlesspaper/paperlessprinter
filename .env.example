# Server
IPP_LISTEN_HOST=0.0.0.0
IPP_LISTEN_PORT=8631
IPP_PATH=/ipp/print
IPP_MAX_BYTES=104857600
IPP_SPOOL_DIR=./spool
IPP_RENDER_DPI=150
IPP_TEMP_DIR=./temp

# Optional exact output size per paper ID. The same profile also controls the
# custom media size advertised to Windows/macOS/CUPS. Keep this JSON on one line
# in Dokploy. A "*" profile can be used as a fallback for unmatched paper IDs.
# Supported fit modes: contain (no crop), cover (center crop), stretch.
IPP_TARGET_PROFILES={}
# IPP_TARGET_PROFILES={"paper-id-one":{"width":1600,"height":1200,"fit":"contain"},"paper-id-two":{"width":800,"height":480,"fit":"contain","auto_rotate":true,"background":"#ffffff"}}

# If true, the server sends an origin-side HTTP 100 Continue response when clients
# use Expect: 100-continue. Some reverse proxies break IPP uploads in that mode,
# so the safer default is false.
IPP_SEND_EXPECT_CONTINUE=false

# Logging
# LOG_LEVEL: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# If true, logs request headers (Authorization/Cookie are redacted)
LOG_HEADERS=false

# Outbound POST
# Leave POST_ENDPOINT empty to disable uploading (store-only mode)
# For PaperlessPaper you can template the endpoint with:
#   <paperId>  or  {PAPER_ID}  or  {paper_id}
# and provide the value via PAPER_ID or via the /ipp/print URL (paper_id=...)
POST_ENDPOINT=

# Optional fallback paper id used to fill POST_ENDPOINT templates. Normally the
# per-printer URL supplies this value; the fallback also covers clients that do
# not preserve the full IPP resource path.
# Can be overridden per request via:
#   /ipp/print?paper_id=123
#   /ipp/print/123
PAPER_ID=

POST_AUTH_HEADER=x-api-key
# Optional fallback credential. A token in /ipp/print/<paper-id>/<token> takes
# precedence. Treat this value and the URL token as secrets.
POST_AUTH_VALUE=REPLACE_ME
POST_TIMEOUT_SECONDS=30

# Multipart form field name for the PNG file
# (set to "picture" for the /uploadSingleImage/... endpoint)
POST_FILE_FIELD=file

# If false, only the PNG file is sent (no extra form fields). PaperlessPaper's
# uploadSingleImage endpoint rejects the optional metadata fields.
POST_INCLUDE_META_FIELDS=false

# If true, send one multipart POST containing all rendered pages instead of only uploading page 1.
POST_SEND_ALL_PAGES=false

# Optional: shared secret expected from clients (basic protection if you expose directly)
# If set, requests must include header: X-IPP-Token: <value>
IPP_SHARED_TOKEN=
